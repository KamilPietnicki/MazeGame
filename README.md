A prototype Mage Game project

Run these commands in your terminal:

| `npm install` | Install dependencies. <br>
| `npm start` | Launch browser to show the game. <br> 
Press `Ctrl + c` to kill **http-server** process.

